import 'package:flutter/material.dart';

class RecordInputScreen extends StatelessWidget {
  const RecordInputScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('기록 입력')),
    );
  }
}
